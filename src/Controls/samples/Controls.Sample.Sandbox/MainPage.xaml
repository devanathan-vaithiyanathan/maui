<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Controls.Sample.MainPage"
             xmlns:local="clr-namespace:Maui.Controls.Sample">
    <VerticalStackLayout Spacing="16"
                         Padding="24"
                         VerticalOptions="Center">
        <Label Text="Issue #4981 – DisplayPromptAsync preselectInitialValue"
               FontAttributes="Bold"
               HorizontalTextAlignment="Center"/>

        <!-- Without preselection (existing behaviour) -->
        <Button x:Name="BtnNoPreselect"
                Text="Prompt WITHOUT preselect (type to append)"
                Clicked="OnNoPreselectClicked"/>

        <!-- With preselection (new behaviour) -->
        <Button x:Name="BtnWithPreselect"
                Text="Prompt WITH preselect (type to overwrite)"
                Clicked="OnWithPreselectClicked"/>

        <!-- Result label -->
        <Label x:Name="ResultLabel"
               Text="Result will appear here"
               HorizontalTextAlignment="Center"/>
    </VerticalStackLayout>
</ContentPage>